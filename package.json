{
  "name": "spg",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "dev": "lerna run --parallel dev",
    "test": "lerna run --parallel test",
    "deploy:web": "git subtree push --prefix packages/website/dist origin gh-pages",
    "deploy:npm": "lerna publish from-package --yes",
    "monoversion": "lerna version"
  },
  "version": "6.0.1-alpha.1"
}
