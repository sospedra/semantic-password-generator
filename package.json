{
  "name": "spg",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn lerna run --parallel dev",
    "build": "yarn lerna run build",
    "test": "yarn lerna run --parallel test"
  }
}
